
This is the Fisher programming language. Everything is based of Fishing or Fishes.
For example:
"fin" = FUNCTION,
"let" = LET,
"catch" = TRUE,
"darn" = FALSE,
"cast" = IF,
"reel" = ELSE,
"return" = RETURN,
"bubbles" = PRINT,